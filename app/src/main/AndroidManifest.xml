<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.textapp">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".notepad.APP"
        android:theme="@style/AppTheme">
        <activity android:name=".notepad.EditMineActivity" />
        <activity android:name=".notepad.RecordActivity"/>
        <activity android:name=".notepad.NotepadActivity">

        </activity>
        <activity android:name=".notepad.NewsActivity"/>
        <activity android:name=".notepad.RegisterActivity" />
        <activity android:name=".notepad.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>